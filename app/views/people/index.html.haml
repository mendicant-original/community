%h1 People

= filter_list(people_path(:format => 'js'))

#people.list
  - @people.each do |person|
    = link_to person_path(person.github) do
      = person.icon
      = person.full_description

= will_paginate @people