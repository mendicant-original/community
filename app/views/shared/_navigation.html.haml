%nav
  %span.title= link_to "Community @ Mendicant University", root_path
  = render :partial => "shared/flash", :locals => { :flash => flash }
  %span.nav
    = link_to "News", "#TODO"
    = link_to "People", people_path
    %span#account
      - if current_user
        = link_to current_user.name, person_path(current_user.github)
        = link_to "Sign out", logout_path
      - else
        = link_to login_path, :class => "btn-auth btn-github" do
          Sign in with <b>Github</b>