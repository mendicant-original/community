- content_for :header_bottom do
  :javascript
    $(function(){
      Filter.init('search', '#{projects_path(:format => 'js')}');
    });

%h1 Projects

.filters
  = search_field_tag :search
  - if current_user
    .controls
      = button_to "New Project", new_project_path,
        :method => :get

#projects.list= render :partial => "projects"